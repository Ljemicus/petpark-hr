# DB APPLY — 2026-07-03

Owner approval: Ljemicus explicitly approved DB apply in Discord `#petpark` on 2026-07-03 14:40 Europe/Zagreb.

## Applied to remote Supabase

Project: `hmtlcgjcxhjecsbmmxol`

Applied directly with `supabase db query --linked` and then marked applied with `supabase migration repair --linked --status applied`:

- `20260703125000_forum_shop_breeder_pet_extras.sql`
- `20260703125500_search_indexes.sql`

## Why direct query, not db push

`supabase db push` was intentionally not used because the repo contained old local migration `20260423140000_rls_least_privilege_hardening.sql` that was not present on remote and referenced legacy schema assumptions (`public.users`, old sitter policies). Applying it now would be risky and likely incompatible with the canonical remote schema.

That legacy file was moved to:

- `supabase/drafts/legacy/20260423140000_rls_least_privilege_hardening.LEGACY_NOT_APPLIED.sql`

## What changed

### Forum/shop/breeder/pet extras

Created missing additive tables aligned to current code, not raw old draft names:

- `forum_topics`
- `forum_comments`
- `products`
- `product_reviews`
- `cart_items`
- `publisher_profiles`
- `litters`
- `puppies`
- `applications`
- `breeder_reviews`
- `breeder_documents`
- `rescue_listings`
- `rescue_appeals`
- `pet_appointments`
- `pet_documents`
- `pet_updates`

RLS was enabled immediately on all new tables.

### Search indexes

Applied additive search indexes:

- `service_listings_city_category_public_idx`
- `service_listings_category_city_public_idx`
- `provider_services_active_service_provider_idx`
- `service_listings_title_trgm_idx`
- `service_listings_description_trgm_idx`

Also ensured `pg_trgm` extension exists in `extensions` schema.

## Verification

Remote checks after apply:

- new tables present: 16
- RLS disabled on new tables: 0
- search indexes present: 5
- policies on new tables: 30
- migration history includes:
  - `20260703125000`
  - `20260703125500`

## Notes

- Raw mobile `00006_mobile_feature_foundation.DRAFT.sql` was not applied because its own header says it is superseded by existing remote schema.
- Raw mobile `00007_forum_shop_breeder_pet_extras.DRAFT.sql` was not applied verbatim because it was not aligned with current web code (`forum_replies` vs `forum_comments`, `price_cents` vs `price`, `profile_id/publisher_type` vs `user_id/type`). A corrected additive migration was applied instead.
- No app deploy was performed.
